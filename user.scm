;; need a way to check the status of our program
;; need a way to quit, optionally saving to file
;; need a way to toggle verbose mode
;; need a way to toggle semi-verbose mode
;; need a way to bind to an internal IP address
;; need a way to bind to an external IP address, with limited capability (pretty much just listening to the stream)
